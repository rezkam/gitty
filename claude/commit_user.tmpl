Generate a Git commit summary for the staged changes. First line: commit message (imperative, <=72 chars). Optional: after a blank line provide a short explanation of what changed and why only when you are certain it is correct. Never use hedging words such as maybe, likely, probably, possibly, might, could, or seems.

Diff:
{{ .Diff }}
